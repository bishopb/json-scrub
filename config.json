{
  "_comment": "Configure the JSON scrubber. This is a comment. They are ignored.",
  "version": 1,
  "keys": {
    "_comment": "`paths` is list of strings, each a dot-separated value representing the key to scrub.",
    "paths": [
    ],
    "_comment": "`global` is list of strings, each representing the key to scrub regardless of where it appears in the object.",
    "global": [
    ]
  }
}
